# crawl-uniswap
repo for interview task

## Run project
Make sure you have mongodb

Run this:

    make run

Or run command: 

    go mod vendor
	go mod tidy
	go vet -all
	go run main.go

Go to localhost:8080